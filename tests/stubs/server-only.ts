// Stub for the `server-only` marker package (aliased in vitest.config.ts).
// The real one throws on import outside a React Server Component build, which
// would make any module carrying the marker untestable.
export {};
